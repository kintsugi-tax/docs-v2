name: Update API Documentation

on:
  # Run every 6 hours to catch API changes
  schedule:
    - cron: '0 */6 * * *'
  
  # Allow manual trigger
  workflow_dispatch:
  
  # Run on relevant code changes
  push:
    branches: [main]
    paths:
      - 'scripts/generate-api-docs.sh'

jobs:
  update-api-docs:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: Install Mintlify scraper
        run: npm install -g @mintlify/scraping
      
      - name: Download OpenAPI specs
        run: |
          echo "📥 Downloading OpenAPI specifications..."
          
          # Download Customer API spec
          echo "📥 Fetching Customer API spec..."
          curl -o openapi.json https://api-doc.trykintsugi.com/openapi.json
          
          # Download Partners API spec  
          echo "📥 Fetching Partners API spec..."
          curl -o openapi-partners.json https://api.trykintsugi.com/openapi.json
          
          echo "✅ OpenAPI specs downloaded successfully!"
      
      - name: Validate OpenAPI specs
        run: |
          echo "🔍 Validating OpenAPI specifications..."
          
          # Validate Customer API spec
          if ! npx @mintlify/scraping validate openapi.json; then
            echo "❌ Customer API spec validation failed"
            exit 1
          fi
          
          # Validate Partners API spec
          if ! npx @mintlify/scraping validate openapi-partners.json; then
            echo "❌ Partners API spec validation failed"
            exit 1
          fi
          
          echo "✅ OpenAPI specs are valid!"
      
      - name: Generate API documentation
        run: |
          echo "🚀 Generating API documentation from OpenAPI specs..."
          mkdir -p reference/api reference/partners
          
          echo "📥 Generating Customer API reference..."
          npx @mintlify/scraping openapi-file openapi.json -o reference/api
          
          echo "📥 Generating Partners API reference..."
          npx @mintlify/scraping openapi-file openapi-partners.json -o reference/partners
          
          echo "✅ API documentation generated successfully!"
      
      - name: Check for changes
        id: changes
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "Changes detected"
            echo "changed=true" >> $GITHUB_OUTPUT
          fi
      
      - name: Commit and push changes
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          
          # Add all changes (OpenAPI files + generated docs)
          git add openapi.json openapi-partners.json reference/api/ reference/partners/
          git commit -m "🤖 Auto-update API documentation and OpenAPI specs [skip ci]

          - Updated openapi.json from https://api-doc.trykintsugi.com/openapi.json
          - Updated openapi-partners.json from https://api.trykintsugi.com/openapi.json  
          - Regenerated API reference documentation
          - Generated on: $(date)"
          git push
          
          echo "✅ API documentation and OpenAPI specs updated and committed"
      
      - name: No changes message
        if: steps.changes.outputs.changed == 'false'
        run: echo "ℹ️ No changes to API documentation"
